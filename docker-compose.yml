version: "3.8"

services:

  flask:
    image: murphs/mtt:flask
    container_name: flask
    restart: always
    expose:
      - 8080
    volumes:
      - ~/shared:/shared


  nginx:
    image: murphs/mtt:nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"

